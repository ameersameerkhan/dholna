{
  "name": "dholna",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "playwright test",
    "smoke:live": "playwright test --config=playwright.live.config.ts",
    "verify:runtime-tracks": "playwright test --config=playwright.track-verification.config.ts --grep @runtime",
    "verify:tracks": "playwright test --config=playwright.track-verification.config.ts"
  },
  "dependencies": {
    "@fontsource/inter": "latest",
    "@fontsource/jetbrains-mono": "latest",
    "@phosphor-icons/react": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "@testing-library/jest-dom": "latest",
    "@testing-library/react": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@types/youtube": "latest",
    "@vitejs/plugin-react": "latest",
    "jsdom": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
